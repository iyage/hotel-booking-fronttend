import React from 'react'
import styled from 'styled-components'

const Container = styled.span`
    
`


function Button({text,pdR,pdL}) {
  return (
    <Container>Button</Container>
  )
}

export default Button